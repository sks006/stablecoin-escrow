[workspace]
members = [
    "native-escrow"
]

# 🛡️ THE FIREWALL
exclude = [
    "anchor-escrow"
]

# The resolver must sit directly inside the [workspace] table
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
overflow-checks = true